import java.util.LinkedList;
import java.util.Queue;

class Pedido {
    // Clase que representa un pedido
    // Puede incluir más atributos según sea necesario
    String id;

    public Pedido(String id) {
        this.id = id;
    }
}

